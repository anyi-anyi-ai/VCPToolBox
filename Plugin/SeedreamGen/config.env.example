# 火山引擎 API 密钥
# 从 https://console.volcengine.com/ark/region:ark+cn-beijing/apiKey 获取
# 支持多个API密钥，用英文逗号分隔，系统会随机选择一个使用
# 如果某个密钥配额用完，会自动尝试其他密钥
# 示例：VOLCENGINE_API_KEY=key1,key2,key3
VOLCENGINE_API_KEY=your_api_key_here

# 是否默认添加水印 (true/false)
DEFAULT_WATERMARK=false

# 默认返回格式 (url 或 b64_json)
# url: 返回图片链接（24小时有效）
# b64_json: 返回Base64编码和url（想让AI看到生成出来的图片可以用这个b64_json，只有多模态AI才能开启这个选项哦！如果AI没有多模态就用url。）
DEFAULT_RESPONSE_FORMAT=url
